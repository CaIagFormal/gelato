spring.application.name=gelato

# Configurações do servidor
server.port=8080
spring.mvc.favicon.enabled=true

# Configurações do banco de dados
spring.datasource.url=jdbc:postgresql://aulasprog.us.to:5432/carloz
spring.datasource.username=postgres
spring.datasource.password=senaipatobranco
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=gelato

# Configurações de JPA e Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

